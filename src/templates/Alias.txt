//------------------------------------------------------------------------------
// Global scope aliases
//------------------------------------------------------------------------------

/**
 * ... 
 */
if (typeof window !== "undefined") {
    if (typeof window.%APP% === "undefined") {
        window.%APP% = %APP%;
    }
}